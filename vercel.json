{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "bun install",
        "buildCommand": "bun run build",
        "framework": "nextjs"
      }
    }
  ]
}